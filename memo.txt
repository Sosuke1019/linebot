やること
 ・面接官としてどんな感じでやっていくかをプロンプトで決めておく
 ・ユーザーに情報を入力してもらう(ES)

出来たら
・ 毎回、ngrokのエンドポイントが変わる(ngrokは起動するたびに異なるエンドポイントを発行するため、
　 一度終了したのちに再度起動した場合はLINE DevelopersのWebhookURLもあわせて修正する必要がある)

linebotをactiveにする手順
 1. source venv/bin/activate で仮想環境をactiveにする
 2. ターミナルでflask run --reload --port 8080を実行する
 3. 別のターミナルでngrok http 8080を実行し、ngrokを起動してサーバーを外部公開する
 4. LINE DeveloperのWebhook URLにForwardingと記載の行のhttpsで始まる側のURLをコピーする

 2023年4月14日の疑問
 main.pyのappとはどれを指す? -> app.pyファイルの app = Flask(__name__) の "app"を指している

参考
 chathptのAPI使ってみる - https://atmarkit.itmedia.co.jp/ait/articles/2303/24/news029.


[__init__.pyについて]
・__init__.py は、ディレクトリ名をモジュール名(あるいは明示的な「名前空間」)としてマッピングするためのファイル、と言える。(ディレクトリ名を名前空間としたときのコンストラクタの役割)
・つまり、モジュールあるいはパッケージの初期化の役割を果たす。

モジュール (Module)
・ファイル単位で分けてプログラムを記載したもの。わかりにくければ、一つの foo.py という Python プログラムファイルが一つの「モジュール」と考えればよい。(これを「pure Python モジュール」と呼ぶようです)。

パッケージ (Package)
・複数のファイル、ディレクトリ構造からなり、ルールに従ってそれらをひと固まりにしたもの4。

名前空間 (Namespace)」
・「モジュール」や「パッケージ」(およびその中)で 使用する「名前」が衝突しないように、使用する名前の空間を分けたもの。階層構造で構成される。


[Pythonでの上位ディレクトリ（親ディレクトリ）や下位ディレクトリ（サブディレクトリ）の表現方法]
　.  で同じディレクトリ
　.. で親ディレクトリ
　... さらに上位層。(.の数に従ってさらに上の階層を表現できる。)
　を表すと分かりました。
